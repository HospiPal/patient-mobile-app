import 'package:flutter/material.dart';

import 'message.dart';

class MessageText {
  String message;
  MessageType messageType;

  MessageText({@required this.message, @required this.messageType});
}
